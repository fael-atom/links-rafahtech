services:
  next-app:
    build:
      context: .
      dockerfile: prod.Dockerfile
    restart: always
    ports:
      - 3000
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.links-rafahtech.rule=Host(`links.rafahtech.com`)"
      - "traefik.http.routers.links-rafahtech.entrypoints=websecure"
      - "traefik.http.routers.links-rafahtech.tls.certResolver=letsencrypt"
      - "traefik.http.services.links-rafahtech.loadbalancer.server.port=3000"
networks:
  dokploy-network:
    external: true
